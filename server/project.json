{
    "build:nodeserver": "ng build && cp server\/* dist",
    "build:nodeserver-prod": "ng build -prod && cp server\/* dist",
    "serve-build": "npm run build:server && cd dist && node index.js",
    "serve-build-prod": "npm run build:server-prod && cd dist && node index.js"
}